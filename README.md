# Push Swap

`push_swap` is a sorting algorithm project from the 42 curriculum focused on algorithmic optimization, low-level programming, and performance-oriented problem solving.

The objective is to sort a list of integers using a restricted set of stack operations while generating the smallest possible number of instructions.

## Result

✅ Final Grade: **100/100**

---

# What This Project Demonstrates

This project emphasizes practical software engineering skills commonly required in systems programming and backend development roles:

- Algorithm design and optimization
- Time complexity analysis
- Data structure implementation
- Memory management in C
- Problem decomposition
- Parsing and validation
- Defensive programming
- Performance-focused development
- Debugging and edge-case handling

---

# Technical Concepts Used

## Data Structures

- Linked lists
- Stacks
- Dynamic memory allocation

## Algorithms

- Cost-based sorting strategies
- Rotation optimization
- Position targeting
- Chunking techniques
- Efficient insertion logic

## Low-Level Programming

- Manual memory management
- Pointer manipulation
- Structured C architecture
- Custom utility functions

---

# Allowed Operations

| Operation | Description |
|---|---|
| `sa` | swap first 2 elements of stack A |
| `sb` | swap first 2 elements of stack B |
| `ss` | execute `sa` and `sb` simultaneously |
| `pa` | push top of B to A |
| `pb` | push top of A to B |
| `ra` | rotate stack A |
| `rb` | rotate stack B |
| `rr` | rotate both stacks |
| `rra` | reverse rotate stack A |
| `rrb` | reverse rotate stack B |
| `rrr` | reverse rotate both stacks |

---

# Compilation

Build the project:

```bash
make
```


```bash
make fclean
```

Rebuild everything:

```bash
make re
```

---

# Usage

```bash
./push_swap 4 2 7 1 9 3
```

Example output:

```text
pb
ra
sa
pa
```

---

# Input Validation

The program handles:

- invalid input
- duplicated numbers
- integer overflow
- non-numeric arguments
- empty input cases

Example:

```bash
./push_swap 1 2 2
```

Output:

```text
Error
```

# Checker Validation

The project can be validated using `checker_linux`, which verifies whether the instructions generated by `push_swap` correctly sort the stack.

## Basic Usage

```bash
ARG="4 67 3 87 23"; ./push_swap $ARG | ./checker_linux $ARG
```

Expected output:

```text
OK
```

If the stack is not sorted or the instructions are invalid:

```text
KO
```

---

# Performance Focus

The project was designed with operation minimization and execution efficiency in mind.

Key optimization goals included:

- reducing unnecessary rotations
- minimizing total instruction count
- selecting optimal insertion paths
- maintaining scalable behavior on large datasets

---

# Skills Strengthened Through This Project

- Writing clean and maintainable C code
- Building efficient algorithms under constraints
- Managing memory safely
- Designing modular architectures
- Handling edge cases and debugging complex logic
- Understanding computational efficiency
- Working with low-level abstractions

---

# Tech Stack

- C
- Makefile
- Unix Environment
- GCC
- Valgrind

---

# Author

Push Swap project completed as part of the 42 Network curriculum.  
Validated with a final score of **100/100**.
